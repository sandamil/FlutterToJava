import 'package:flutter/material.dart';

class NuevaApp extends StatelessWidget {
  // const NuevaApp({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text('Nueva app'))));
  }
}
